/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ArticleCreateComponentComponent } from './article-create-component.component';

describe('ArticleCreateComponentComponent', () => {
  let component: ArticleCreateComponentComponent;
  let fixture: ComponentFixture<ArticleCreateComponentComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ArticleCreateComponentComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ArticleCreateComponentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/

