/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ThemeDetailComponentComponent } from './theme-detail-component.component';

describe('ThemeDetailComponentComponent', () => {
  let component: ThemeDetailComponentComponent;
  let fixture: ComponentFixture<ThemeDetailComponentComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ThemeDetailComponentComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ThemeDetailComponentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
