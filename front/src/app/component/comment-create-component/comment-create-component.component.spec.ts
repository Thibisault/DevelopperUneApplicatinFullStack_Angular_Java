/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CommentCreateComponentComponent } from './comment-create-component.component';

describe('CommentCreateComponentComponent', () => {
  let component: CommentCreateComponentComponent;
  let fixture: ComponentFixture<CommentCreateComponentComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CommentCreateComponentComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(CommentCreateComponentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
