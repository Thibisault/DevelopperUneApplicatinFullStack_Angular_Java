/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ThemeListComponentComponent } from './theme-list-component.component';

describe('ThemeListComponentComponent', () => {
  let component: ThemeListComponentComponent;
  let fixture: ComponentFixture<ThemeListComponentComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ThemeListComponentComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ThemeListComponentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/