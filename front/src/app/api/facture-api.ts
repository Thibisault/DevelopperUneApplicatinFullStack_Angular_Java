//path: src/app/api/facture-api.ts
import { API_BASE_URL } from './api-config';

export const FACTURE_API_ROUTES = {
  base: `${API_BASE_URL}/api/facture`,
  login: "/login",
  register: '/register',
};